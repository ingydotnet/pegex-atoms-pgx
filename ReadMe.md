Pegex Grammar for its Atom Rules
================================

This is the default atoms rules that will work with most languages. It is PCRE
based. This grammar may be extended for languages with different regex engines.
